diff -Nur -x '*.orig' -x '*.rej' e-smith-base-4.17.0/root/etc/e-smith/events/actions/purge-old-logs mezzanine_patched_e-smith-base-4.17.0/root/etc/e-smith/events/actions/purge-old-logs
--- e-smith-base-4.17.0/root/etc/e-smith/events/actions/purge-old-logs	2005-11-20 21:28:05.000000000 -0700
+++ mezzanine_patched_e-smith-base-4.17.0/root/etc/e-smith/events/actions/purge-old-logs	2007-01-13 13:01:56.000000000 -0700
@@ -30,11 +30,13 @@
 #----------------------------------------------------------------------
 # Find all files in /var/log/ that are:
 #    - real files
-#    - older than 35 days
+#    - older than syslog{PurgeLength}, or 95 days if unset
 #    - matches our log naming scheme
 # and eliminate them.
 #----------------------------------------------------------------------
 
+my $length = $syslog{PurgeLength} || '95';
+
 chdir "/var/log/"
     or die "Could not chdir to /var/log";
 
@@ -43,7 +45,7 @@
     #------------------------------------------------------------------
     # Files older than 35 days, matching our naming scheme.
     #------------------------------------------------------------------
-    if ( -f and (int(-M) > 35) and /^[A-Za-z_].*\.\d{14}$/ )
+    if ( -f and (int(-M) > $length) and /^[A-Za-z_].*\.\d{14}$/ )
     {
 	unlink("$_")
 	    or die "Could not purge log $File::Find::name: $!\n";
